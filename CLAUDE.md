# AlugaQuadra (TCC)

Laravel 12 + Livewire 4 + Fortify + Pest, PostgreSQL via `docker-compose.yml`.
O plano de trabalho fica em `PLANO.md`.

## Convenções

- Commits: somente no nome de Matheus-danno. Nunca adicionar `Co-Authored-By`,
  "Generated with", ou crédito de IA.
- Mensagem de commit: imperativo, sem ponto final, sem prefixo `feat:/fix:`
  (siga o estilo de `git log --oneline`).
